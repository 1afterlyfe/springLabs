BEGIN
EXECUTE IMMEDIATE 'DROP TABLE tasks';
EXCEPTION
   WHEN OTHERS THEN
      IF SQLCODE != -942 THEN
         RAISE;
END IF;
END;
/

CREATE TABLE tasks (
                       id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                       title VARCHAR2(255) NOT NULL,
                       due_date DATE,
                       priority VARCHAR2(50) NOT NULL,
                       completed NUMBER(1) DEFAULT 0
);

INSERT INTO tasks (title, due_date, priority, completed)
VALUES ('Підготувати звіт', TO_DATE('2025-12-01', 'YYYY-MM-DD'), 'High', 0);

COMMIT;